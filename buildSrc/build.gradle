// Configure sensible layout
sourceSets {
	main {
		groovy {
			srcDirs = ['src']
		}
		resources {
			srcDirs = ['resources']
		}
	}

	test {
		groovy {
			srcDirs = ['test']
		}
		resources {
			srcDirs = ['resources']
		}
	}
}

apply plugin: 'eclipse'

tasks.eclipse.dependsOn(cleanEclipse)

